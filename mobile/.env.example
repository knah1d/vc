# Use your computer's LAN IP for a physical phone, or a public HTTPS backend.
# Leave blank during local Expo development to use the Metro host on port 4000.
EXPO_PUBLIC_API_URL=""

# Get a DSN from https://sentry.io (new React Native project). Crash reporting
# is disabled with a console warning if left unset.
EXPO_PUBLIC_SENTRY_DSN=""
